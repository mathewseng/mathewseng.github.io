backend: basic-pitch
normalize:
  min_duration_sec: 0.03
  min_pitch: 21
  max_pitch: 108
notes:
  description: >
    Baseline transcription config. Install optional dependencies with
    `pip install -e '.[transcription]'` to enable the backend.
